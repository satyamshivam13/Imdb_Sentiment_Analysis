---
phase: 01-analytics-data-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
requirements: [HIST-01]
requirements_addressed: [HIST-01]
files_modified:
  - requirements.txt
  - tests/test_history_store.py
  - tests/test_persistence_integration.py
  - .planning/phases/01-analytics-data-foundation/01-VALIDATION.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Phase 1 storage behavior is covered by automated checks."
    - "Schema and persistence integration regressions are detectable by fast test commands."
  artifacts:
    - path: tests/test_history_store.py
      provides: "schema and query primitive tests"
    - path: tests/test_persistence_integration.py
      provides: "endpoint-to-persistence integration checks"
  key_links:
    - from: tests/test_persistence_integration.py
      to: app.py
      via: "Flask test client calls `/predict` and `/api/predict` and validates persisted event outcomes"
---

# Phase 1 Plan 03: Validation and Integrity Coverage

## Objective

- **What:** Add automated coverage and validation strategy updates for persistence foundation behavior.
- **Why:** Phase completion must be verifiable and resistant to silent regression before batch/dashboard work starts.
- **Output:** Test scaffolding and Phase 1 validation document aligned to executable checks.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-analytics-data-foundation/01-VALIDATION.md
@.planning/phases/01-analytics-data-foundation/01-RESEARCH.md

<objective>
Add automated tests and validation controls that protect the persistence foundation from regressions.
</objective>

## Tasks

<task type="code">
  <name>Ensure pytest test infrastructure is available</name>
  <files>requirements.txt</files>
  <read_first>requirements.txt, .planning/phases/01-analytics-data-foundation/01-VALIDATION.md</read_first>
  <action>Add `pytest` to `requirements.txt` if it is not already present, keeping existing package pins intact and avoiding unrelated dependency changes.</action>
  <acceptance_criteria>`requirements.txt` contains `pytest`; existing runtime dependencies from pre-change file remain present</acceptance_criteria>
  <verify>Run `rg "^pytest" requirements.txt`.</verify>
  <done>Project can execute automated tests for persistence behavior in this phase.</done>
</task>

<task type="code">
  <name>Add unit tests for schema and storage primitives</name>
  <files>tests/test_history_store.py</files>
  <read_first>storage/history_store.py, .planning/REQUIREMENTS.md</read_first>
  <action>Create `tests/test_history_store.py` covering schema initialization idempotency, event insertion, sentiment count aggregation, and day-based trend grouping using temporary test database files.</action>
  <acceptance_criteria>`tests/test_history_store.py` exists; file contains tests for schema init, insert behavior, sentiment counts, and trend grouping</acceptance_criteria>
  <verify>Run `python -m pytest tests/test_history_store.py -q`.</verify>
  <done>Core storage logic is automatically validated with passing unit tests.</done>
</task>

<task type="code">
  <name>Add endpoint integration persistence tests and refresh validation doc</name>
  <files>tests/test_persistence_integration.py, .planning/phases/01-analytics-data-foundation/01-VALIDATION.md</files>
  <read_first>app.py, tests/test_history_store.py, .planning/phases/01-analytics-data-foundation/01-VALIDATION.md</read_first>
  <action>Create `tests/test_persistence_integration.py` using Flask test client to confirm successful `/predict` and `/api/predict` calls persist rows, then update `01-VALIDATION.md` status fields/maps so commands and file references match implemented tests.</action>
  <acceptance_criteria>`tests/test_persistence_integration.py` exists; file contains tests that call `/predict` and `/api/predict`; `01-VALIDATION.md` includes both test files and current command references</acceptance_criteria>
  <verify>Run `python -m pytest tests/test_persistence_integration.py -q` and `python -m pytest -q`.</verify>
  <done>Integration path is verified and validation strategy document reflects real executable checks.</done>
</task>

## Verification

- Run `python -m pytest -q`.
- Confirm validation document references current tests and commands.

## Success Criteria

- [ ] Storage foundation has automated unit + integration regression coverage.
- [ ] Validation strategy is synchronized with actual test artifacts and commands.
