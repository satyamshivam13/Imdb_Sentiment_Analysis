---
phase: 05-history-experience-hardening
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [HIST-02]
requirements_addressed: [HIST-02]
files_modified:
  - app.py
  - storage/history_store.py
  - templates/history.html
  - templates/base.html
  - static/css/app.css
  - tests/test_history_page.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Users can open a dedicated /history page and browse prior analysis events in chronological order."
    - "The history view uses server-side pagination with previous/next navigation and a stable page size."
    - "Each history row shows the key details needed to scan past predictions quickly without expanding extra UI."
  artifacts:
    - path: app.py
      provides: "History browse route and pagination wiring"
    - path: storage/history_store.py
      provides: "Paginated latest-events retrieval for the history page"
    - path: templates/history.html
      provides: "Server-rendered history table and pagination controls"
    - path: tests/test_history_page.py
      provides: "Render and pagination coverage for the history page"
  key_links:
    - from: app.py
      to: storage/history_store.py
      via: "History route requests a paginated event slice from the store"
    - from: app.py
      to: templates/history.html
      via: "Server-rendered history page receives the paginated event list"
    - from: templates/base.html
      to: app.py
      via: "Shared shell includes a discoverable link to the /history route"
---

<objective>
Build the server-rendered history browsing surface for prior analysis events.

Purpose: Phase 5 needs a simple, readable way to inspect prior predictions before adding destructive controls.
Output: /history page, server-side pagination, shared navigation, and page tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-history-experience-hardening/05-CONTEXT.md
@.planning/phases/04-model-metrics-observatory/04-CONTEXT.md
@app.py
@storage/history_store.py
@templates/base.html
@static/css/app.css

<interfaces>
The Phase 5 context locks the history experience into a server-rendered page with server-side pagination.
Treat `HistoryStore.latest_events()` as the retrieval anchor and extend it only as needed to support `limit` and `offset` parameters. The page should render newest-first rows with timestamp, review snippet, sentiment label, confidence, and source, while keeping the layout visually aligned with the existing glass-panel product shell.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add paginated history retrieval and browse route</name>
  <files>storage/history_store.py, app.py</files>
  <action>Extend the history store so `latest_events()` can return a paginated slice using `limit` and `offset`, then add a `/history` route in `app.py` that reads `page` from the query string, loads the current page of events, and passes pagination metadata into the template. Keep the response server-rendered and newest-first so the history page stays consistent with the rest of the app.
  </action>
  <verify><automated>rg "latest_events\(|/history|page_size|offset|limit" storage/history_store.py app.py</automated></verify>
  <done>The app can render a paginated history page backed by the existing SQLite store.</done>
</task>

<task type="auto">
  <name>Task 2: Build the history page layout and navigation shell</name>
  <files>templates/history.html, templates/base.html, static/css/app.css</files>
  <action>Create a dedicated history template that presents a flat newest-first table with timestamp, truncated review snippet, sentiment label, confidence, and source. Add shared-shell navigation so the page is discoverable from the app header, and reuse the existing glass-panel styling rather than introducing a new visual language. Keep pagination controls simple and readable at the bottom of the table.</action>
  <verify><automated>rg "history|pagination|review snippet|sentiment|confidence|source" templates/history.html templates/base.html static/css/app.css</automated></verify>
  <done>The history UI is readable, server-rendered, and visually aligned with the dashboard and metrics pages.</done>
</task>

<task type="auto">
  <name>Task 3: Add page tests for browse behavior and pagination hooks</name>
  <files>tests/test_history_page.py</files>
  <action>Write tests that confirm the `/history` route renders the table, exposes pagination controls, and preserves newest-first ordering assumptions against a fixture-backed store. Focus the assertions on route presence, visible row fields, and page navigation rather than snapshotting the entire HTML.</action>
  <verify><automated>python -m pytest tests/test_history_page.py -q</automated></verify>
  <done>The browse surface is regression-covered and ready for the clear-history follow-up plan.</done>
</task>

</tasks>

<verification>
- Run `python -m pytest tests/test_history_page.py -q`.
- Confirm `/history` shows newest-first analysis rows with pagination controls.
- Confirm the shared shell includes a discoverable history link.
</verification>

<success_criteria>
- [ ] A dedicated `/history` page exists and loads from the shared app shell.
- [ ] History entries render in a compact newest-first table with pagination.
- [ ] Page-level tests exist for the browse surface.
</success_criteria>

<output>
After completion, create `.planning/phases/05-history-experience-hardening/05-01-SUMMARY.md`.
</output>
