---
phase: 05-history-experience-hardening
plan: 02
type: execute
wave: 2
depends_on: [05-01]
requirements: [HIST-03]
requirements_addressed: [HIST-03]
files_modified:
  - app.py
  - storage/history_store.py
  - templates/history.html
  - tests/test_history_clear.py
  - .planning/phases/05-history-experience-hardening/05-VALIDATION.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Users can clear all local history from the interface after an explicit confirmation step."
    - "The clear action is implemented as a POST route that removes the stored analysis events."
    - "After clearing, the history page reflects the empty state without requiring a new feature or workflow."
  artifacts:
    - path: app.py
      provides: "POST /history/clear destructive action wiring"
    - path: storage/history_store.py
      provides: "History table deletion support"
    - path: tests/test_history_clear.py
      provides: "Confirmation and deletion coverage for the clear-history action"
    - path: .planning/phases/05-history-experience-hardening/05-VALIDATION.md
      provides: "Nyquist map for the history clear action"
  key_links:
    - from: app.py
      to: storage/history_store.py
      via: "Clear route delegates to the history store deletion method"
    - from: app.py
      to: templates/history.html
      via: "Clear action and confirmation control live on the same history page"
    - from: tests/test_history_clear.py
      to: app.py
      via: "Contract tests exercise the POST clear route and resulting empty state"
---

<objective>
Add the destructive history clearing flow and its verification coverage.

Purpose: Phase 5 finishes the history experience by giving users a simple, intentional way to remove local history.
Output: Clear-history POST route, store deletion support, confirmation flow, and action tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-history-experience-hardening/05-CONTEXT.md
@.planning/phases/05-history-experience-hardening/05-01-SUMMARY.md
@app.py
@storage/history_store.py
@templates/history.html

<interfaces>
Plan 02 depends on the history browse surface already existing from Plan 01. The clear action should stay intentionally simple: a POST route from the history page, a confirmation prompt in the UI, and a table-clear operation in the SQLite store. Keep the browse page intact and let the clear flow collapse it back to the empty-state experience.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a destructive clear-history route and store method</name>
  <files>storage/history_store.py, app.py</files>
  <action>Add a method on the history store that deletes all analysis events, then expose it through a `POST /history/clear` route in `app.py`. Keep the route explicit and intentional, returning the user to the history page after the delete succeeds so the empty-state behavior is obvious.
  </action>
  <verify><automated>rg "clear|delete|/history/clear|POST" storage/history_store.py app.py</automated></verify>
  <done>The app can delete all local history through a dedicated POST route.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the confirmation control into the history page</name>
  <files>templates/history.html, app.py</files>
  <action>Place a clear-history control on the existing history page and connect it to a simple browser confirmation step before the POST is submitted. Keep the interaction lightweight and consistent with the rest of the app, with no soft-delete or recovery UI in v1.</action>
  <verify><automated>rg "confirm\(|Clear History|history/clear" templates/history.html app.py</automated></verify>
  <done>The clear action is visible on the history page and cannot fire without user confirmation.</done>
</task>

<task type="auto">
  <name>Task 3: Add deletion tests and validation scaffolding</name>
  <files>tests/test_history_clear.py, .planning/phases/05-history-experience-hardening/05-VALIDATION.md</files>
  <action>Write tests that exercise the clear-history POST route, assert the destructive action empties the store, and verify the page returns to the empty-state experience afterward. Add the phase validation map so the history clear behavior is explicitly tied to its test coverage.</action>
  <verify><automated>python -m pytest tests/test_history_clear.py -q</automated></verify>
  <done>The clear-history behavior is regression-covered and documented in the phase validation map.</done>
</task>

</tasks>

<verification>
- Run `python -m pytest tests/test_history_clear.py -q`.
- Confirm `POST /history/clear` empties the SQLite history table.
- Confirm the history page shows the empty state after deletion.
</verification>

<success_criteria>
- [ ] Users can clear all local history from the interface.
- [ ] The clear action is explicit, destructive, and POST-based.
- [ ] Tests and validation scaffolding cover the history deletion flow.
</success_criteria>

<output>
After completion, create `.planning/phases/05-history-experience-hardening/05-02-SUMMARY.md`.
</output>
