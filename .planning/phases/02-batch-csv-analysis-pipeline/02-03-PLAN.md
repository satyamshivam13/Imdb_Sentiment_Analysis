---
phase: 02-batch-csv-analysis-pipeline
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
requirements: [BATCH-04, BATCH-05]
requirements_addressed: [BATCH-04, BATCH-05]
files_modified:
  - services/batch_service.py
  - app.py
  - templates/batch_result.html
  - templates/home.html
  - static/js/app.js
  - tests/test_csv_validation.py
  - tests/test_batch_integration.py
  - .planning/phases/02-batch-csv-analysis-pipeline/02-VALIDATION.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Users can download an enriched CSV containing original row data plus sentiment columns."
    - "Batch responses include aggregate totals and percentages for positive and negative outcomes."
    - "Batch export and report flows are covered by integration tests and synchronized in VALIDATION.md."
  artifacts:
    - path: services/batch_service.py
      provides: "Export and metrics helpers for enriched output and aggregate summaries"
    - path: tests/test_batch_integration.py
      provides: "Endpoint-level coverage for upload, report, and export workflows"
    - path: .planning/phases/02-batch-csv-analysis-pipeline/02-VALIDATION.md
      provides: "Nyquist verification map aligned to implemented test commands"
  key_links:
    - from: app.py
      to: templates/batch_result.html
      via: "Batch analyze route renders report summary and row diagnostics"
---

# Phase 2 Plan 03: Batch Reporting, Export, and Validation Sync

## Objective

- **What:** Complete aggregate reporting and downloadable enriched CSV outputs.
- **Why:** Phase 2 is only complete when stakeholders can interpret batch outcomes and export enriched results.
- **Output:** Report UI/API metrics, export endpoint, and test/validation alignment.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-batch-csv-analysis-pipeline/02-CONTEXT.md
@.planning/phases/02-batch-csv-analysis-pipeline/02-RESEARCH.md
@.planning/phases/02-batch-csv-analysis-pipeline/02-VALIDATION.md
@.planning/phases/02-batch-csv-analysis-pipeline/02-02-PLAN.md

<objective>
Add batch report summary and export endpoint/UI.
</objective>

## Tasks

<task type="code">
  <name>Add aggregate metrics and enriched CSV export helpers</name>
  <files>services/batch_service.py, app.py</files>
  <read_first>services/batch_service.py, app.py, .planning/REQUIREMENTS.md</read_first>
  <action>Implement `BatchService.build_summary(scored_rows, invalid_rows)` returning `total_rows`, `valid_rows`, `invalid_rows`, `positive_count`, `negative_count`, `positive_pct`, and `negative_pct`. Implement `BatchService.build_enriched_csv(original_headers, scored_rows)` that appends `sentiment_label`, `sentiment_value`, and `confidence` columns. Add `GET /batch/export/<export_id>` in `app.py` returning `text/csv` download response built from stored batch results.</action>
  <acceptance_criteria>`services/batch_service.py` contains `def build_summary`; `services/batch_service.py` contains `def build_enriched_csv`; `app.py` contains `@app.get("/batch/export/<export_id>")`; exported CSV includes `sentiment_label,sentiment_value,confidence` columns</acceptance_criteria>
  <verify>Run `rg "def build_summary|def build_enriched_csv|batch/export/<export_id>|sentiment_label|sentiment_value|confidence" services/batch_service.py app.py`.</verify>
  <done>Core reporting and export helpers satisfy BATCH-04 and BATCH-05 output contracts.</done>
</task>

<task type="code">
  <name>Deliver report rendering and download UX in templates</name>
  <files>templates/batch_result.html, templates/home.html, static/js/app.js, app.py</files>
  <read_first>templates/home.html, static/js/app.js, app.py</read_first>
  <action>Create `templates/batch_result.html` to display aggregate metrics, row-level issues, and download action linking to `/batch/export/<export_id>`. Update `batch_analyze_post` in `app.py` to render `batch_result.html` with summary and diagnostics context. Keep `templates/home.html` and `static/js/app.js` aligned so batch submission routes users to report view with clear status messaging.</action>
  <acceptance_criteria>`templates/batch_result.html` exists; file contains placeholders for `positive_count`, `negative_count`, and `invalid_rows`; file contains export link using `batch_export`; `app.py` contains `render_template("batch_result.html"`</acceptance_criteria>
  <verify>Run `rg "batch_result.html|positive_count|negative_count|invalid_rows|batch_export" templates/batch_result.html app.py`.</verify>
  <done>Stakeholders can read batch summary metrics and download enriched output through the web UI.</done>
</task>

<task type="code">
  <name>Add integration tests and finalize validation strategy references</name>
  <files>tests/test_csv_validation.py, tests/test_batch_integration.py, .planning/phases/02-batch-csv-analysis-pipeline/02-VALIDATION.md</files>
  <read_first>app.py, services/batch_service.py, .planning/phases/02-batch-csv-analysis-pipeline/02-VALIDATION.md</read_first>
  <action>Create `tests/test_csv_validation.py` for schema/alias and row validation guardrails, and `tests/test_batch_integration.py` for analyze plus export endpoint behavior (including mixed-result and download response assertions). Update `02-VALIDATION.md` status rows and commands to match actual test file names and mark wave readiness for execution verification.</action>
  <acceptance_criteria>`tests/test_csv_validation.py` exists; `tests/test_batch_integration.py` exists; integration tests call `/batch/analyze` and `/batch/export/<export_id>`; `02-VALIDATION.md` references both new test files and current pytest commands</acceptance_criteria>
  <verify>Run `python -m pytest tests/test_csv_validation.py -q`, `python -m pytest tests/test_batch_integration.py -q`, and `python -m pytest -q`.</verify>
  <done>Batch export/report behaviors are regression-protected and Nyquist validation map is synchronized to implemented checks.</done>
</task>

## Verification

- Run `python -m pytest tests/test_csv_validation.py -q`.
- Run `python -m pytest tests/test_batch_integration.py -q`.
- Run `python -m pytest -q`.

## Success Criteria

- [ ] Enriched CSV export is downloadable with required sentiment columns.
- [ ] Aggregate metrics expose positive/negative totals and percentages.
- [ ] Validation and test artifacts are synchronized for execution.
