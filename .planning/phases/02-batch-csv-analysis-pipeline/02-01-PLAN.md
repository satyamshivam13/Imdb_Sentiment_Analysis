---
phase: 02-batch-csv-analysis-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [BATCH-01, BATCH-02]
requirements_addressed: [BATCH-01, BATCH-02]
files_modified:
  - app.py
  - services/batch_service.py
  - services/__init__.py
  - templates/home.html
  - static/js/app.js
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Users can submit CSV files through UI/API and receive immediate validation feedback for unsupported payloads."
    - "Schema validation enforces a canonical `review` field and reports actionable errors with row and field context."
  artifacts:
    - path: services/batch_service.py
      provides: "Batch ingestion and validation primitives for CSV files"
    - path: app.py
      provides: "Batch validation routes for HTML and JSON flows"
  key_links:
    - from: app.py
      to: services/batch_service.py
      via: "`BatchService` import and usage in batch validation routes"
    - from: templates/home.html
      to: app.py
      via: "Batch upload form posts file payload to batch analyze endpoint"
---

# Phase 2 Plan 01: CSV Upload and Validation Foundation

## Objective

- **What:** Establish batch upload entrypoints and strong schema/row validation.
- **Why:** Reliable batch analysis depends on predictable input contracts and clear validation diagnostics.
- **Output:** Batch service validation layer with UI/API upload flows and actionable error reporting.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-batch-csv-analysis-pipeline/02-CONTEXT.md
@.planning/phases/02-batch-csv-analysis-pipeline/02-RESEARCH.md

<objective>
Build CSV upload and schema validation flow.
</objective>

## Tasks

<task type="code">
  <name>Create batch CSV parsing and validation service primitives</name>
  <files>services/batch_service.py, services/__init__.py</files>
  <read_first>services/history_service.py, app.py, .planning/phases/02-batch-csv-analysis-pipeline/02-CONTEXT.md</read_first>
  <action>Create `services/batch_service.py` with `class BatchService` and methods `parse_csv_upload(file_storage)`, `normalize_review_column(fieldnames)`, and `validate_rows(rows, max_chars)`; implement alias normalization from `reviews`, `review_text`, and `text` to canonical `review`; enforce `.csv` extension and UTF-8/UTF-8-SIG decode path; return structured validation issues with `row`, `field`, and `reason`. Add `from .batch_service import BatchService` to `services/__init__.py`.</action>
  <acceptance_criteria>`services/batch_service.py` contains `class BatchService`; `services/batch_service.py` contains `def parse_csv_upload`; `services/batch_service.py` contains `def normalize_review_column`; `services/batch_service.py` contains `def validate_rows`; `services/__init__.py` contains `from .batch_service import BatchService`</acceptance_criteria>
  <verify>Run `rg "class BatchService|def parse_csv_upload|def normalize_review_column|def validate_rows" services/batch_service.py`.</verify>
  <done>Batch parsing and validation contract exists with canonical schema handling and actionable issue format.</done>
</task>

<task type="code">
  <name>Wire batch validation endpoints for HTML and API flows</name>
  <files>app.py</files>
  <read_first>app.py, services/batch_service.py, .planning/REQUIREMENTS.md</read_first>
  <action>Add route handlers in `app.py` for `POST /batch/analyze` (form upload) and `POST /api/batch/analyze` (multipart upload). Instantiate `BatchService` at module scope, parse uploaded file from field name `reviews_file`, run schema/row validation, and return validation-first responses without scoring yet. JSON responses must use explicit status codes (`400` for validation errors, `200` for accepted payloads).</action>
  <acceptance_criteria>`app.py` contains `@app.post("/batch/analyze")`; `app.py` contains `@app.post("/api/batch/analyze")`; `app.py` contains `BatchService`; `app.py` contains `reviews_file`; `app.py` contains validation issue response fields including `row` and `reason`</acceptance_criteria>
  <verify>Run `rg "batch/analyze|api/batch/analyze|BatchService|reviews_file|row|reason" app.py`.</verify>
  <done>Batch upload requests can be accepted and validated through both UI and API surfaces.</done>
</task>

<task type="code">
  <name>Add batch upload UI controls and client-side affordances</name>
  <files>templates/home.html, static/js/app.js</files>
  <read_first>templates/home.html, static/js/app.js, app.py</read_first>
  <action>Update `templates/home.html` to include a separate batch upload form targeting `/batch/analyze` with file input name `reviews_file` and `accept=.csv`; add status/error placeholders for upload validation feedback. Extend `static/js/app.js` to show selected filename and disable batch submit button while upload is in progress.</action>
  <acceptance_criteria>`templates/home.html` contains `action="{{ url_for('batch_analyze_post') }}"`; `templates/home.html` contains `name="reviews_file"`; `templates/home.html` contains `accept=".csv"`; `static/js/app.js` contains batch form submission handling</acceptance_criteria>
  <verify>Run `rg "batch_analyze_post|reviews_file|accept=\".csv\"" templates/home.html` and `rg "reviews_file|batch" static/js/app.js`.</verify>
  <done>Users can discover and submit batch CSV uploads from the primary UI with clear interaction feedback.</done>
</task>

## Verification

- Run `python -m compileall app.py services`.
- Run `python -m pytest -q` and note any failing tests unrelated to this plan.

## Success Criteria

- [ ] CSV upload entrypoints exist for both HTML and API paths.
- [ ] Validation errors are actionable and include row/field-level diagnostics.
