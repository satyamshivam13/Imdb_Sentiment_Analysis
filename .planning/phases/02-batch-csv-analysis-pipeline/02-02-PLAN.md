---
phase: 02-batch-csv-analysis-pipeline
plan: 02
type: execute
wave: 2
depends_on: [02-01]
requirements: [BATCH-03]
requirements_addressed: [BATCH-03]
files_modified:
  - services/batch_service.py
  - app.py
  - tests/test_batch_service.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Every valid batch row is scored with sentiment label, numeric value, and confidence using existing model behavior."
    - "Invalid rows remain visible in output diagnostics while valid rows continue processing."
    - "Successfully scored rows are persisted to history with source `batch`."
  artifacts:
    - path: services/batch_service.py
      provides: "Batch scoring pipeline with mixed-result output contract"
    - path: tests/test_batch_service.py
      provides: "Service-level coverage for row scoring and mixed-result behavior"
  key_links:
    - from: app.py
      to: services/batch_service.py
      via: "Batch routes call BatchService analyze_rows and return per-row results"
---

# Phase 2 Plan 02: Batch Scoring Pipeline and Row Outputs

## Objective

- **What:** Implement scoring for valid rows and return mixed-result outputs.
- **Why:** Batch value depends on transforming validated rows into model-backed predictions while preserving error visibility.
- **Output:** Service pipeline that scores rows, persists valid events, and returns row-level result objects.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-batch-csv-analysis-pipeline/02-CONTEXT.md
@.planning/phases/02-batch-csv-analysis-pipeline/02-RESEARCH.md
@.planning/phases/02-batch-csv-analysis-pipeline/02-01-PLAN.md

<objective>
Implement batch scoring pipeline with row-level outputs.
</objective>

## Tasks

<task type="code">
  <name>Implement mixed-result scoring contract in BatchService</name>
  <files>services/batch_service.py</files>
  <read_first>services/batch_service.py, app.py, .planning/phases/02-batch-csv-analysis-pipeline/02-CONTEXT.md</read_first>
  <action>Add method `analyze_rows(rows, predict_fn, max_chars)` returning a dictionary with `valid_rows`, `invalid_rows`, `scored_rows`, and `issues`. For each valid row, call `predict_fn(review_text)` and append per-row result fields `row_index`, `review`, `sentiment_label`, `sentiment_value`, and `confidence`. Keep invalid rows in `issues` with original row index and reason.</action>
  <acceptance_criteria>`services/batch_service.py` contains `def analyze_rows`; `services/batch_service.py` contains `scored_rows`; `services/batch_service.py` contains `row_index`; `services/batch_service.py` contains `sentiment_label`; `services/batch_service.py` contains `confidence`</acceptance_criteria>
  <verify>Run `rg "def analyze_rows|scored_rows|row_index|sentiment_label|confidence" services/batch_service.py`.</verify>
  <done>Batch service can produce deterministic row-level prediction outputs while preserving validation issues.</done>
</task>

<task type="code">
  <name>Persist successful batch rows and return row-level results in routes</name>
  <files>app.py</files>
  <read_first>app.py, services/batch_service.py, services/history_service.py</read_first>
  <action>Update `batch_analyze_post` and `api_batch_analyze` in `app.py` to call `BatchService.analyze_rows(..., predict_sentiment, MAX_REVIEW_CHARS)` and persist each scored row through `history_service.append_prediction_event` with `source="batch"`. Ensure persistence failures are logged and do not hide scoring output; keep invalid-only responses as validation failures.</action>
  <acceptance_criteria>`app.py` contains `analyze_rows`; `app.py` contains `source="batch"`; `app.py` contains `append_prediction_event`; `app.py` contains row-level response payload for scored rows</acceptance_criteria>
  <verify>Run `rg "analyze_rows|source=\"batch\"|append_prediction_event|scored_rows" app.py`.</verify>
  <done>Batch routes emit row-level predictions and write successful rows to persistent history.</done>
</task>

<task type="code">
  <name>Add service-level tests for mixed-result scoring behavior</name>
  <files>tests/test_batch_service.py</files>
  <read_first>services/batch_service.py, tests/test_history_store.py, tests/test_persistence_integration.py</read_first>
  <action>Create `tests/test_batch_service.py` covering alias normalization, mixed valid/invalid rows, and scored row payload shape. Include at least one test asserting that invalid rows do not appear in `scored_rows` and that confidence/label fields are present for valid rows.</action>
  <acceptance_criteria>`tests/test_batch_service.py` exists; file contains tests for alias normalization and mixed-result scoring; file asserts `scored_rows` output shape</acceptance_criteria>
  <verify>Run `python -m pytest tests/test_batch_service.py -q`.</verify>
  <done>Batch scoring contract has focused automated coverage before export/report work begins.</done>
</task>

## Verification

- Run `python -m pytest tests/test_batch_service.py -q`.
- Run `python -m pytest -q`.

## Success Criteria

- [ ] Valid rows are scored with label/value/confidence.
- [ ] Mixed-result behavior preserves invalid-row diagnostics without aborting valid-row scoring.
- [ ] Successful scored rows are persisted with source `batch`.
